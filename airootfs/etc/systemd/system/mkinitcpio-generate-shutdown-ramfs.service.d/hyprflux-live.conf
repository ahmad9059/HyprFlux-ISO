[Unit]
# The generated shutdown ramfs loops in initrd emergency mode on this live ISO.
# The installer explicitly syncs, unmounts the target, disables swap, and uses
# a direct kernel reboot, so generating a userspace shutdown ramfs is neither
# required nor safe in the live environment.
ConditionPathExists=/run/hyprflux-enable-shutdown-ramfs
